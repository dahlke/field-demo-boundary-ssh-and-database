slug: boundary-ssh-and-database
id: adnyt8negprv
type: track
title: Using Boundary for SSH and Database Access
teaser: TODO
description: TODO
icon: https://storage.googleapis.com/instruqt-frontend/assets/hashicorp/tracks/boundary.png
tags:
- terraform
owner: hashicorp
developers:
- neil@hashicorp.com
private: true
published: false
show_timer: true
challenges:
- slug: install-boundary
  id: wyaxsa25ysd1
  type: challenge
  title: Install Boundary
  teaser: Boundary must first be installed on your machine.
  notes:
  - type: text
    contents: TODO
  assignment: |-
    *Set up your development environment*

    ```
    curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo apt-key add -
    ```

    ```
    sudo apt-add-repository "deb [arch=amd64] https://apt.releases.hashicorp.com $(lsb_release -cs) main"
    ```

    ```
    sudo apt-get update && sudo apt-get install boundary
    ```

    _TODO: Start up Vault?_

    Start up Postgres.

    ```
    docker run --name postgres -p 5432:5432 -e POSTGRES_PASSWORD=postgres -d postgres
    ```

    Update the Postgres connection string in `/root/boundary-controller.hcl`.

    Configure Postgres.

    ````
    PGPASSWORD=postgres psql -U postgres -h localhost -p 5432 -c "CREATE DATABASE boundary";
    PGPASSWORD=postgres psql -U postgres -h localhost -p 5432 -c "CREATE USER boundary WITH PASSWORD 'boundary' SUPERUSER;"
    ````

    Initialize the database in Postgres.

    ```
    boundary database init -config=/root/boundary-controller.hcl
    ```

    ```
    ```
  tabs:
  - title: VS Code
    type: service
    hostname: workstation
    port: 8443
  - title: Terminal
    type: terminal
    hostname: workstation
  difficulty: advanced
  timelimit: 1800
checksum: "2864917334374464205"
